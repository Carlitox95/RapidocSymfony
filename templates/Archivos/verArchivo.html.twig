{% extends 'base.html.twig' %}
 
{% block head %} 
   {{ parent() }}    
  
  {% block javascripts %}
    <!-- Funciones Javascript -->
    <script src="{{ asset('js/DocumentoController.js') }}"></script>      
    <!-- Funciones Javascript -->
  {% endblock javascripts %}
{% endblock head %}
                
{% block contenido %}
<div class="separadorMain"> 

  <!-- Mensaje de Seleccion del Documento -->
    <div class="offset-s1 col s10 offset-s1">
      <nav>
        <div class="nav-wrapper blue lighten-2 white-text">
          <div class="col s12">
           <a href="{{ path('archivos') }}" class="breadcrumb">Archivos</a>          
           <a href="{{ path('archivosPersona',{'idPersona': documento.persona.id}) }}" class="breadcrumb">Directorio de {{documento.persona.apellido}},{{documento.persona.nombre}}: {{documento.persona.cuitcuil}}</a>
           <a href="#!" class="breadcrumb">{{documento.nombre}}</a>
          </div>
        </div>
      </nav>
   <br>
   <br>
   </div>
  <!-- Mensaje de Seleccion del Documento --> 
  

  
    
  <!-- Muestro el Archivo -->
  <div class="offset-s2 col s8 offset-s2">         
   <iframe width="100%"  height="900px" src="{{ asset(documento.getRuta()) }}" frameborder="0" border="0" cellspacing="0" style="border-style: none;"></iframe>  
   <br>
   <br>
  </div>
  <!-- Muestro el Archivo -->
  


  <br>
  <!-- Botones de Direccionamiento -->
  <div class="offset-s2 col s8 offset-s2">
    <center> 
      <a href="{{ path('archivos') }}" class="waves-effect waves-light btn-large blue lighten-2 white-text">
       <i class="material-icons left">assignment</i>Volver a los Archivos
      </a> 
      <a href="{{ path('eliminarArchivo',{'idDocumento': documento.id}) }}" class="waves-effect waves-light btn-large red"><i class="material-icons left">delete</i>Eliminar Archivo</a>
    </center>
  </div>
  <!-- Botones de Direccionamiento -->
  


</div>
{% endblock contenido %}            

